# 论文图片与数据核验说明（2026-09-25）

核验对象：`脑转移瘤局部进展演化的两阶段潜空间扩散模型DiT.docx`（修改结果已直接覆盖该文件；更新前原稿归档于 `核验材料/原稿备份/`）。
核验方式：用当前 notebook 代码（`脑肿瘤预测_DiT算法.ipynb`）+ 项目内 checkpoint（`brain_tumor_ldm.pt`，md5 见下）在真实数据（105 例训练 / 51 例测试）上重跑，所有数值均为实跑结果，不引用任何无法追溯的旧数字。

## 一、结论

1. **图片**：论文原有 11 张图中，8 张与当前代码运行产物**逐字节（md5）一致**；图1 是流程示意图（图注已标"示意"）；图3、图4 是 Mets_030 的真实切片（与原始 PNG 支撑集完全相同、相关系数 0.9995，仅显示灰度映射不同）。**未发现伪造图片**。
2. **数据**：表2、表3、表4、表5 及图7–图10 的全部数字都能由 51 例复算逐位复现（复算脚本见 `核验材料/`）。**唯一不真实的是原表1 的训练损失**（自编码器 0.3162→0.0626、DiT 1.0590→0.6219）：项目内没有任何训练产物支持这组数字，且原文自述"未固定随机种子、无法独立复算"。已用实跑替换。
3. 按要求，**摘要配图与正文图6 的预测图已替换为你提供的 150 天结果**（经比对识别为 Mets_020、Mets_049、Mets_042 三例）。

## 二、图片逐张核验结果

| 论文位置 | 媒体文件 | 对应运行产物 | 核验结果 |
|---|---|---|---|
| 摘要配图 | `image1.png` | `fig_front_multicase_grid.png`（旧图） | 本次已替换为三例 150 天结果 |
| 图1 模型框架示意 | `image2.png` | 手绘示意图 | 示意图，图注已标明；宽高比已校正 |
| 图2 生长律权重曲线 | `image5.png` | `fig_growth_weight_curves.png` | md5 完全一致 |
| 图3 Mets_030 z=136 | `image3.png` | `BRAINMETASTASIS/train/Mets_030/t1_gd/t1_gd_z136.png` | 真实切片（相关 0.9995，支撑集相同） |
| 图4 Mets_030 z=131 | `image4.png` | `t1_gd_z131.png` | 真实切片 |
| 图5 重建示例 | `image6.png` | `fig_reconstruction_example.png` | md5 完全一致（Mets_001，MSE 0.011349、PSNR 19.451） |
| 图6 进展模拟 | `image7.png` | `fig_progression_multplane.png`（旧图） | 本次已替换 |
| 图6(b)(c) | `image12/13.png` | 本次新增 | 用户提供的 150 天结果 |
| 图7 指标分布 | `image8.png` | `fig_metric_distribution.png` | md5 完全一致 |
| 图8 中间帧对比 | `image9.png` | `fig_growthlaw_visual.png` | md5 完全一致 |
| 图9 负荷轨迹 | `image10.png` | `fig_ablation_burden_trajectory.png` | md5 完全一致 |
| 图10 负荷—权重线性度 | `image11.png` | `fig_burden_affinity.png` | md5 完全一致（残差 6.20e-16） |

## 三、新预测图（150 天）的来源与核验

- 三张新图是 cell 14（`PREDICT_DAYS = 150`，`plot_full_progression(max_steps=8, show_lesion_box=False)`）的输出：8 个时间点 Day 0/21/42/64/85/107/128/150，三行分别为轴向、冠状、矢状切面。
- **病例识别**：把每张图的 Day 0 切面与 51 例测试集逐例比对，最佳匹配为 Mets_020（相关 0.9957）、Mets_049（0.9960）、Mets_042（0.9966），其余病例均 ≤0.94。
- **可复现性**：用当前代码 + 桌面 checkpoint（`/Users/chris/Desktop/brain_tumor_ldm.pt`）重跑，图形与提供的一致；150 天指标（种子 20260925）：

| 病例 | 软轮廓 IoU | 高频纹理相关性 | 病灶区平均灰度变化 |
|---|---|---|---|
| Mets_020 | 0.9680 | 0.9816 | −0.0232 |
| Mets_049 | 0.9845 | 0.9805 | −0.0259 |
| Mets_042 | 0.9874 | 0.9803 | −0.0229 |

- 图中黑色区域是 `_blackout_tumor_growth` 按生长律进度涂黑的病灶候选区域（Day 0 不涂黑、Day 150 涂黑程度最高），属显示方式，不改变数值。

## 四、训练损失（原表1）实跑结果

在真实训练集上按 notebook 的 `train()` 原样训练（105 例 × 4 种增强 = 420 步/轮，批量 1，Adam，lr 1e-4，CPU / PyTorch 2.8.0）：

| 轮次 | 自编码器重建损失 L_VAE（种子20260925） | DiT 噪声预测损失（种子20260925） | DiT（种子20260926） |
|---|---|---|---|
| 第1轮 | 0.1260 | 0.9335 | 0.9165 |
| 第2轮 | 0.0647 | 0.6279 | 0.4762 |
| 第3轮 | 0.0650 | 0.4828 | 0.3664 |
| 第4轮 | —（自编码器仅 3 轮） | 0.4184 | 0.2989 |
| 第5轮 | — | 0.3731 | 0.2673 |

- 自编码器其中 MSE 分量：0.0566 → 0.0279 → 0.0279（种子20260925）。
- 单次训练耗时约 356–364 秒；逐轮损失随随机种子变化，但两次运行的收敛趋势一致（DiT 单调下降）。
- 原始记录：`核验材料/训练/history_种子20260925.json`、`history_种子20260926.json`、`train_种子20260925.log`。

## 五、其它数据核验（51 例复算）

- 重建（项目内 checkpoint）：MSE 0.026584±0.008190、MAE 0.061806±0.011001、PSNR 15.986±1.494 dB；逐例 PSNR 13.85–19.45 dB —— 与表2、正文一致。
- 进展模拟（100 天）：软轮廓 IoU 0.9836±0.0047、阈值 IoU 0.9881±0.0035、纹理相关性 0.9808±0.0012、病灶区灰度变化 −0.0219±0.0023 —— 与表3一致。
- 生长律消融：指数律最大偏差 0.1865、V(100)/V(0)=4.666、T_d=45.0 天；logistic-FKPP 0.2093、V(100)/V(0)=1.699 —— 与表4一致。
- 两份 checkpoint 对比：桌面 checkpoint 重建 PSNR 15.993 dB（项目内 15.986 dB），**全部进展指标逐位相同**（100 天序列像素级一致）。
- 逐例结果：`核验材料/51例复算/复算_项目内checkpoint_*.csv`、`复算_桌面checkpoint_summary.json`。

## 六、本次对 docx 的改动清单

1. 摘要配图：替换为三例 150 天轴向条带（含病例标注）。
2. 图6：替换为三张 150 天图（图6(a) Mets_020、图6(b) Mets_049、图6(c) Mets_042）及三条图注。
3. §4.3 正文相应改写（150 天跨度、8 个时间点、涂黑说明、三例实测指标）。
4. 表1：改为三列（轮次 / 自编码器 L_VAE / DiT L_LDM），数值为实跑结果；图注注明种子与"自编码器仅 3 轮"。
5. §4.1 正文：改为固定种子复现训练的描述（含 MSE 分量、降幅 60.0%、两种子复现说明）。
6. §5：原"表1的原始训练损失无法独立复算"改为"表1的训练损失来自固定种子的复现训练，逐轮数值随随机种子变化，只反映收敛趋势"。
7. 图1 框架示意图显示宽高比恢复为原始比例。
8. 标题改为「从单帧影像到演化轨迹：脑转移瘤局部进展的两阶段潜空间扩散生成框架」，文档属性标题同步；
9. 摘要重写：突出随访判读的临床困境与本文两项关键设计（两阶段自适应噪声注入 / 生长律参数化时间推进），直接罗列数值改为结论性表述（约27万参数、51例、软轮廓IoU与纹理相关性约0.98），并保留数据边界声明。摘要由 1150 字压缩至 761 字，所有表述均可在正文与 `核验材料/` 中逐条对应（参数 274,485、IoU 0.9836、纹理 0.9808、命题残差见 §4.5、多种子逐位一致）。
10. 其余正文、表格、图片未改动。

## 七、复现命令

```bash
# 51 例复算（约 45 秒 / 份 checkpoint）
cd /private/tmp/verify_exp
CKPT=<checkpoint.pt> OUT=<输出目录> /Applications/Xcode.app/Contents/Developer/usr/bin/python3 run_experiments.py
# 训练复现（约 6 分钟 / 次）
SEED=20260925 OUT=<输出目录> /Applications/Xcode.app/Contents/Developer/usr/bin/python3 run_training.py
# 150 天三例复现 + 出图（约 9 秒 / 例）
SEED=20260925 CASES=Mets_020,Mets_049,Mets_042 /Applications/Xcode.app/Contents/Developer/usr/bin/python3 repro_newfigs.py
```


## 八、指标口径核对（第二轮：PSNR 与损失）

**结论：代码里的 ≈35 dB 与论文的 15.99 dB 是两个不同的量，两者都真实，但不可互换。**

| 量 | 定义 | 51 例实测（项目内 checkpoint） |
|---|---|---|
| 重建 PSNR（论文表2） | 整幅 64×64×64 体数据、灰度 [0,1]、PSNR = −10·lg MSE | MSE 0.026584±0.008190 → 逐例 PSNR **15.986±1.494 dB**（13.85–19.45 dB） |
| 重建 PSNR（仅脑区体素） | 同上，仅统计脑区 | MSE 0.157 → 约 8.0 dB |
| 重建 PSNR（仅背景体素） | 同上，仅统计背景 | MSE 5.2×10⁻⁵ → 约 42.9 dB |
| 重建 PSNR（显示归一化切片） | 每切片 1–99 百分位拉伸后再比 | MSE 0.0887 → 约 10.5 dB |
| 代码 cell 14 打印的“PSNR 35.55 dB” | **预测末帧与源帧之差**（切片级 MSE，非重建） | MSE 2.0–3.5×10⁻⁴ → 34.6–37.0 dB |

- 逐切片全幅口径与整幅体数据口径的均值严格相等（切片等大），代码中的随机切片采样是同一量的无偏估计；论文统一报告整幅体数据口径。
- 该 PSNR 在脑区口径下更低（约 8 dB）、在背景口径下更高（约 43 dB），整幅口径并非“美化”数值。
- 复算脚本与原始结果：`核验材料/指标口径/metric_audit.py`、`metric_audit.json`、`psnr_origin.py`、`psnr_origin.json`。

**本轮论文改动**（均已写入论文文件）：
1. §3.3 明确 PSNR 定义（−10·lg MSE、灰度 [0,1]、整幅口径）及其与逐切片/随机采样口径的关系。
2. §4.2 增加口径说明：脑区占比 16.9%、背景 MSE 5.2×10⁻⁵、脑区口径约 8.0 dB，并说明代码预测环节打印的 ≈35 dB（34.6–37.0 dB）是“改动幅度”而非重建质量。
3. §5 结论中的重建 PSNR 标注为“整幅体数据口径”。
4. 表1 去掉自编码器一列，仅保留 DiT 噪声预测损失（0.9335/0.6279/0.4828/0.4184/0.3731，种子 20260925），题注同步。
5. §4.1 相应改写：不再报告自编码器训练损失，说明其重建质量由第4.2节的 MSE/MAE/PSNR 给出；DiT 损失随种子变化（20260926 复跑 0.9165→0.2673）仍如实保留。

## 九、仍需注意

- `T_d = 45` 天与生长律衰减常数仍是**未标定的占位值**（论文 §4.4 已声明），正式投稿前需给出文献或队列依据。
- `脑肿瘤预测_DiT算法.ipynb` cell 14 的已存输出仍是旧的生长先验（等效倍增 93.6 天），与论文口径不一致，建议清空输出或重跑后再截图。
- 本机没有 LibreOffice，且无法调用 Word 自动化，因此未生成逐页 PDF 校样；docx 结构（13 张图宽高比、关系引用、表格）已用程序校验通过，建议在 Word 中打开确认分页与图注排版。
