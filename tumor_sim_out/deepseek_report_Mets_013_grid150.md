**生成设置**

本病例为 Mets_013 的脑转移瘤进展模拟，时间网格共 N = 150 个刻度，刻度 0 为输入影像，刻度 150 为扩散模型生成的终点。生长律设定为 exponential，进度空间为 volume，等效体积倍增时间 T_d = 45.0 天，该参数为生长律参数且未由队列标定。终点与起始的体积比为 V(N)/V(0) = 10.079。

**时间推进**

时间权重与线性对照的最大偏差为 0.2696，说明推进过程并非匀速线性。逐刻度插值权重抽样显示：Step0=0.000、Step30=0.065、Step60=0.167、Step90=0.330、Step120=0.589、Step150=1.000，权重随刻度递增但增速前慢后快。需要明确指出，中间帧是按上述生长律进行非均匀插值得到的生成结果，不代表真实随访影像。

**结构保持**

脑轮廓 soft IoU 为 0.9815，高频纹理相关性为 0.9836，两项指标均处于较高水平。候选病灶区均值变化为 -0.0217，反映该区域在生成过程中的整体灰度偏移幅度较小。展示切片的 MSE=0.0004、MAE=0.0091、PSNR_dB=33.8296，可作为该切片重建误差的定量参考。

**局限性**

T_d = 45.0 天为生长律参数且未由队列标定，因此体积增长的时间尺度不具备队列层面的验证基础。时间权重与线性对照存在 0.2696 的偏差，意味着各中间刻度的形态推进依赖所设生长律，而非真实观测。所有中间帧均为生成结果，不能视为真实随访，上述结构保持与重建误差指标也仅基于给定数值，不构成临床判断依据。
